{
  "name": "email-permutation-guesser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Guess & verify the most likely corporate email address for a contact (first name, last name, .lu domain) using a MillionVerifier -> Prospeo -> Blitz fallback chain.",
  "scripts": {
    "generate-contacts": "tsx scripts/generate-contacts.ts",
    "guess": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
