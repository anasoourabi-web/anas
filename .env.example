# --- MillionVerifier (primary) ---
# https://app.millionverifier.com/  ->  API
MILLIONVERIFIER_API_KEY=

# --- Prospeo (fallback #1) ---
# https://prospeo.io/api  ->  Email Verifier
PROSPEO_API_KEY=

# --- Blitz (fallback #2) ---
# TODO: paste the real endpoint + key once you share the Blitz API docs.
BLITZ_API_KEY=
BLITZ_API_URL=

# --- Runtime tuning (all optional) ---
# Milliseconds to wait between verification calls (be nice to the APIs / your credits).
REQUEST_DELAY_MS=350
# How many verification attempts to retry on transient network/5xx errors.
MAX_RETRIES=3
